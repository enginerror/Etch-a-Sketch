const sketch_area = document.querySelector("#sketch-area");

const sketchAreaSize = 450;
let sideValue = 16;

sketch_area.style.height = `${sketchAreaSize}px`;
sketch_area.style.width = `${sketchAreaSize}px`;